<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>My First Webpage</title>
  <meta name="description"
    content="My first website — a simple HTML & CSS page showcasing a hero, about section and contact form." />
  <!-- Open Graph -->
  <meta property="og:title" content="My First Webpage" />
  <meta property="og:description"
    content="My first website — a simple HTML & CSS page showcasing a hero, about section and contact form." />
  <meta property="og:type" content="website" />
  <link rel="icon" href="/favicon.ico" />

  <style>
    :root {
      --color-bg: #d7bfa7;
      --color-primary: #0077cc;
      --color-primary-dark: #005fa3;
      --color-contrast: #ffffff;
      --color-muted: #444444;
      --nav-height: 4.5rem;
      --radius: 0.625rem;
      --max-width: 72ch;
      --gap: 1.25rem;
    }

    /* Reset default margins */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    html {
      font-family: Arial, sans-serif;
      font-size: 16px;
      background-color: var(--color-bg);
      color: #333;
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
    }

    /* Layout container */
    .container {
      max-width: var(--max-width);
      margin: 0 auto;
      padding: 0 1rem;
    }

    /* Visually hidden helper for screen readers */
    .sr-only {
      position: absolute;
      width: 1px;
      height: 1px;
      padding: 0;
      margin: -1px;
      overflow: hidden;
      clip: rect(0 0 0 0);
      white-space: nowrap;
      border: 0;
    }

    /* Global focus outline for keyboard users */
    a:focus,
    button:focus,
    input:focus,
    textarea:focus {
      outline: 3px solid rgba(0, 119, 204, 0.25);
      outline-offset: 2px;
    }

    /* HEADER / NAVIGATION */
    header {
      background-color: var(--color-primary);
      color: var(--color-contrast);
      position: sticky;
      top: 0;
      z-index: 1000;
    }

    nav {
      display: flex;
      justify-content: space-between;
      align-items: center;
      height: var(--nav-height);
      padding: 0 1rem;
    }

    nav h2 {
      font-size: 1.25rem;
      color: var(--color-contrast);
      margin-left: 0.25rem;
    }

    nav ul {
      list-style: none;
      display: flex;
      gap: 1.5rem;
      align-items: center;
    }

    nav a {
      text-decoration: none;
      color: var(--color-contrast);
      font-weight: 700;
      font-size: 1.05rem;
      transition: color 0.2s ease, transform 0.15s;
    }

    nav a:hover,
    nav a:active {
      color: #cfeeff;
      transform: translateY(-1px);
    }

    /* SKIP LINK */
    .skip-link {
      position: absolute;
      left: 0;
      top: -40px;
      background: var(--color-primary);
      color: var(--color-contrast);
      padding: 0.5rem 1rem;
      z-index: 2000;
      text-decoration: none;
      border-bottom-right-radius: 0.5rem;
      border-top-right-radius: 0.5rem;
    }

    .skip-link:focus {
      top: 0.5rem;
    }

    /* HERO SECTION */
    .hero {
      text-align: center;
      padding: 4rem 1rem;
    }

    .hero img {
      width: 18.75rem;
      /* 300px */
      max-width: 40%;
      border-radius: 50%;
      margin-top: 1.25rem;
      box-shadow: 0 0.25rem 0.625rem rgba(0, 0, 0, 0.12);
      display: inline-block;
      object-fit: cover;
    }

    .hero h1 {
      font-size: 2rem;
      color: var(--color-primary);
      margin-bottom: 1.25rem;
    }

    .hero p {
      font-size: 1.125rem;
      color: var(--color-muted);
    }

    /* ABOUT SECTION */
    .about {
      padding: 3.5rem 1rem;
      text-align: center;
    }

    .about h2 {
      color: var(--color-primary);
      font-size: 1.5rem;
      margin-bottom: 0.75rem;
    }

    .about p {
      margin: 0 auto;
      line-height: 1.6;
      font-size: 1.125rem;
      color: var(--color-muted);
    }

    /* CONTACT SECTION */
    .contact {
      background-color: #e6f2ff;
      padding: 2.5rem 1rem;
      text-align: center;
    }

    .contact h2 {
      color: var(--color-primary);
      margin-bottom: 1rem;
      font-size: 1.5rem;
    }

    form {
      max-width: 34rem;
      margin: 0 auto;
      padding: 1.75rem;
      border-radius: var(--radius);
      box-shadow: 0 0.25rem 0.625rem rgba(0, 0, 0, 0.08);
      background: #fff;
    }

    form input,
    form textarea {
      width: 100%;
      padding: 0.75rem;
      margin: 0.5rem 0;
      border: 1px solid #cfcfcf;
      border-radius: 0.375rem;
      font-size: 1rem;
      outline: none;
    }

    form input:focus,
    form textarea:focus {
      border-color: var(--color-primary);
      box-shadow: 0 0 0 3px rgba(0, 119, 204, 0.08);
    }

    form button {
      background-color: var(--color-primary);
      border: none;
      border-radius: 0.5rem;
      cursor: pointer;
      color: var(--color-contrast);
      padding: 0.75rem 1.25rem;
      transition: background-color 0.2s;
      font-size: 1rem;
      margin-top: 0.5rem;
    }

    form button:hover {
      background-color: var(--color-primary-dark);
    }

    #form-status {
      margin-top: 0.75rem;
      min-height: 1.25rem;
      font-size: 0.95rem;
    }

    #form-status.success {
      color: green;
    }

    #form-status.error {
      color: #b00020;
    }

    /* FOOTER */
    footer {
      background-color: var(--color-primary);
      color: var(--color-contrast);
      text-align: center;
      padding: 1rem;
      margin-top: 1.25rem;
    }

    /* RESPONSIVE */
    @media (max-width: 600px) {
      nav ul {
        gap: 0.5rem;
      }

      .hero img {
        width: 10rem;
      }

      .hero h1 {
        font-size: 1.5rem;
      }

      .about p,
      .hero p {
        font-size: 1rem;
      }
    }
  </style>
</head>

<body>
  <a class="skip-link" href="#main">Skip to main content</a>

  <header>
    <nav aria-label="Primary navigation" class="container">
      <h2>MyWebsite</h2>
      <ul>
        <li><a href="#home">Home</a></li>
        <li><a href="#about">About</a></li>
        <li><a href="#contact">Contact</a></li>
      </ul>
    </nav>
  </header>

  <main id="main" class="container" role="main">
    <!-- HERO SECTION -->
    <section class="hero" id="home" aria-labelledby="hero-heading">
      <h1 id="hero-heading">Welcome to My First Website!</h1>
      <p>Learning HTML &amp; CSS is fun</p>
      <img src="images/divine.hotboy.jpg" alt="Profile photo of Divine" loading="lazy" />
    </section>

    <!-- ABOUT SECTION -->
    <section class="about" id="about" aria-labelledby="about-heading">
      <h2 id="about-heading">About Me</h2>
      <p>
        Hello! I'm just starting my web development journey.
        This is my first webpage — built with care using HTML and CSS.
        My goal is to become a professional front-end developer soon.
      </p>
    </section>

    <!-- CONTACT SECTION -->
    <section class="contact" id="contact" aria-labelledby="contact-heading">
      <h2 id="contact-heading">Contact Me</h2>

      <form id="contact-form" action="https://formspree.io/f/mgvpwpbq" method="POST">
        <!-- Screen-reader labels kept (visually hidden) -->
        <label class="sr-only" for="name">Your name</label>
        <input id="name" name="name" type="text" placeholder="Your name" required />

        <label class="sr-only" for="email">Your email</label>
        <input id="email" name="email" type="email" placeholder="Your email" required />

        <label class="sr-only" for="message">Your message</label>
        <textarea id="message" name="message" rows="5" placeholder="Your message" required></textarea>

        <button type="submit">Send Message</button>

        <div id="form-status" role="status" aria-live="polite"></div>
      </form>
    </section>
  </main>

  <!-- FOOTER -->
  <footer>
    <p>© 2025 My First Website. All rights reserved.</p>
  </footer>

  <script>
    // Progressive enhancement: if JS is available we intercept the submit,
    // otherwise the form will submit normally to Formspree.
    (function () {
      const form = document.getElementById('contact-form');
      const status = document.getElementById('form-status');
      if (!form) return;

      form.addEventListener('submit', async function (e) {
        // Prevent default only when JS is enabled (progressive enhancement).
        e.preventDefault();
        status.textContent = 'Sending…';
        status.className = '';

        const url = form.action;
        const formData = new FormData(form);

        try {
          const res = await fetch(url, {
            method: form.method,
            body: formData,
            headers: {
              'Accept': 'application/json'
            }
          });

          if (res.ok) {
            status.textContent = 'Thanks! Your message has been sent.';
            status.classList.add('success');
            form.reset();
          } else {
            // Try to parse error details from Formspree
            let message = 'There was a problem sending your message.';
            try {
              const data = await res.json();
              if (data && data.error) message = data.error;
            } catch (_) { /* ignore */ }
            status.textContent = message;
            status.classList.add('error');
            // Move focus to status so screen reader users are alerted
            status.focus && status.focus();
          }
        } catch (err) {
          status.textContent = 'Network error. Please try again later.';
          status.classList.add('error');
          status.focus && status.focus();
        }
      });
    })();
  </script>
</body>

</html>
